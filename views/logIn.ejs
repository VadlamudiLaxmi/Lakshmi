<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      background-image: url('/images/pexels-daria-obymaha-1684151.jpg'); /* Background image */
      background-size: cover;
      background-position: center;
      height: 100vh; /* Adjust height to fit the screen */
    }
    header {
      background-color: rgba(255, 255, 255, 0.8); /* Background color with opacity */
      padding: 20px;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 999; /* Ensure the header is above other content */
      display: flex;
      justify-content: space-between;
      align-items: center; /* Align items vertically */
    }
    .logo {
      width: 100px; /* Adjust width as needed */
      height: auto; /* Maintain aspect ratio */
    }
    .navigation {
      margin-top: 20px;
    }
    .navigation a {
      margin: 0 10px;
      color: #333;
      text-decoration: none;
    }
    .container {
      max-width: 400px;
      margin: 200px auto; /* Adjust margin-top to make space for the header */
      background-color: #fff;
      border-radius: 8px;
      padding: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    h1 {
      text-align: center;
      color: #333;
    }
    form {
      text-align: center;
    }
    input[type="email"],
    input[type="password"],
    button[type="submit"] {
      width: 100%;
      margin-bottom: 10px;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }
    button[type="submit"] {
      background-color: #007bff;
      color: #fff;
      cursor: pointer;
    }
    button[type="submit"]:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <header>
    <img src="/images/Untitled-design copy.jpg" alt="Logo" class="logo"> <!-- Replace 'Untitled-design copy.png' with your actual logo file path -->
    <nav class="navigation">
      <a href="/home">Home</a> | <a href="/about">About</a> | <a href="/contact">Contact Us</a> | <a href="/users/register">Register</a>
    </nav>
  </header>
  <div class="container">
    <h1>Login</h1>
    <% if (error_msg) { %>
      <p style="color: red;"><%= error_msg %></p>
    <% } %>
    <form action="/users/login" method="post">
      <input type="email" name="email" placeholder="Email" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit">Login</button>
      <a href="/users/register">Sign Up</a> <!-- Signup link -->
    </form>
  </div>
</body>
</html>